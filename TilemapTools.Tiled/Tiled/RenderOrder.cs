namespace TilemapTools.Tiled
{
    /// <summary>
    /// The order in which tiles are rendered. In all cases, the map is drawn row-by-row.
    /// </summary>
    public enum RenderOrder
    {
        RightDown,
        RightUp,
        LeftDown,
        LeftUp
    }
}